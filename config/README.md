# Config

> [!Caution]
>
> Do not use constants outside of `nuxt.config.ts`, if you should use them in other places, please expose them via `runtimeConfig` & `useRuntimeConfig` instead.

Shared constants used by `nuxt.config.ts`.
